spring:
  application:
    name: bis-master-data-service
  datasource:
    url: jdbc:postgresql://localhost:5432/bisdb
    username: bisuser
    password: bispassword
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: health,info
